# v0072 POSITION RECONCILE + CLEAN DISPLAY

Что исправлено:
- `/positions` теперь перед выводом сверяет локальный кэш с реальными позициями MEXC.
- Убраны дубли реальных позиций, которые MEXC может вернуть через разные endpoints/форматы символа.
- Реальные exchange-only позиции автоматически импортируются в local cache без ручного `/recovery`.
- Старые локальные позиции, которых уже нет на бирже, удаляются из кэша, чтобы не было каши LONG/SHORT и старых строк.
- Добавлена защита от ложной очистки: если MEXC не вернул строку позиции, но баланс показывает margin/PnL, локальные строки не удаляются и показывается hidden-margin warning.
- В `/positions` добавлена строка `Sync cleanup`, чтобы было видно, что бот восстановил/обновил/очистил.
- Версия обновлена на `0072 POSITION RECONCILE + CLEAN DISPLAY`.

Проверка:
- `pytest -q` → 68 passed.
